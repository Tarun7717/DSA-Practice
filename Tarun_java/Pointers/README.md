The pointer is used to store the address of another variable.
 We have to store the variable inside a pointer vriable
 Then we can use the pointer variable to print the address of the variable as well as the value of the variable using '&'.

 POINTERS TO AN ARRAY:
 Here we are using pointers with the array.
